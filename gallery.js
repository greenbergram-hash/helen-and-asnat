const links=[...document.querySelectorAll('.art-link')];const dialog=document.querySelector('dialog');let current=0;function show(i){current=(i+links.length)%links.length;const source=links[current].querySelector('img');document.querySelector('#large-art').src=links[current].href;document.querySelector('#large-art').alt=source.alt;document.querySelector('#art-label').textContent=source.alt;document.querySelector('#counter').textContent=`${current+1} / ${links.length}`;}links.forEach((a,i)=>a.addEventListener('click',e=>{e.preventDefault();show(i);dialog.showModal();}));document.querySelector('#close').onclick=()=>dialog.close();document.querySelector('#previous').onclick=()=>show(current-1);document.querySelector('#next').onclick=()=>show(current+1);dialog.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();show(current-1);}if(e.key==='ArrowRight'){e.preventDefault();show(current+1);}});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});